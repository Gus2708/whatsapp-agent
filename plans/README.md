# Implementation Plans

Generated by the `improve` skill on 2026-06-12, audited against commit `a5262a1`.
Updated 2026-06-17 — merged `advisor/improve-bundle`, added plans 006–008 (audit
against commit `a3f5b0d`). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix `supabase_schema.sql` to match prod (`tazas`, not `tasas`) | P1 | S | — | DONE |
| 002 | Re-enable TLS verification for the n8n container | P2 | S | — | DONE |
| 003 | Verification baseline: extract pure search logic into a tested `lib/` | P1 | M | — | DONE |
| 004 | Make `lib/` the single source of truth + drift guard | P2 | S | 003 | DONE |
| 005 | Regenerate the stale `n8n_workflow.json` + drift guard | P1 | M | 004 | DONE |
| 006 | Set `N8N_LOG_LEVEL=warn` (was `debug`) in production | P1 | S | — | DONE |
| 007 | Guard employee-app dependencies in `supabase_schema.sql` | P1 | S | — | DONE |
| 008 | Parallelize `hacer_presupuesto` item lookups with `Promise.all` | P2 | M | — | DONE |
| 009 | Fix CRLF bug in `check_sources_sync.js` guard | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Execution log

### 2026-06-12 (plans 001–005)

All five plans were executed by a dispatched executor subagent in an isolated git
worktree and reviewed by the advisor.

- **Branch**: `advisor/improve-bundle` (`fd9db16`→`a111962`, base `a5262a1`).
- **Merged into master**: 2026-06-17 at `a3f5b0d` (clean merge, no conflicts).
- **Two environment notes baked into the guards**:
  1. This box runs **Node v25** — use `node --test` (not `node --test tests/`).
  2. CRLF checkout: guards normalize CRLF→LF before comparing (`.replace(/\r\n/g,'\n')`).

## Execution log — 2026-06-17 (plans 006–009)

All four plans executed; three of them need a `git merge` into master before they take effect:

- **006** (log level) + **009** (CRLF guard): applied directly to master at `1670584`.
  Require `docker compose restart n8n` to take effect in the running container.
- **007** (schema guards): branch `advisor/007-schema-dependency-guards`, commit `f7e3777`.
  To merge: `git merge advisor/007-schema-dependency-guards`
- **008** (parallel presupuesto): branch `advisor/008-parallelize-presupuesto`, commit `cb8440c`.
  To merge: `git merge advisor/008-parallelize-presupuesto`. After merging, import
  the updated `n8n_workflow.json` into the live n8n instance.

Notes:
- Worktrees were based on `68812ea` (remote HEAD), not `1670584` (local HEAD after the
  `advisor/improve-bundle` merge). Tests were unavailable in worktrees for 007 (they
  exist only after the merge commit). The 006 fix was verified in the main checkout.
- A pre-existing `check_sources_sync.js` failure on all worktrees was caused by the
  CRLF bug in the guard (plan 009). Fixed in `1670584`.
- **007 done criterion note**: the count of `REFERENCES public.profiles` stays at 3
  (they're now inside DO $$ blocks, not removed); that criterion was a spec error.
  The correct check is no bare FK in CREATE TABLE statements.

## Dependency notes

- 004 required 003 (lib/ created in 003) — both DONE.
- 005 required 004 — DONE.
- 008 uses `scripts/build_workflow.js` created by 004 — must be DONE before 008.

## Findings not yet planned (available for a future `improve` run)

- **S1 — `anon` role full write on `productos`, `ventas`, `tazas`, `clientes`**:
  cross-repo fix (backend serrucho writes via anon too); coordinate before planning.
- **S3 — Docker socket mounted into n8n** (`docker-compose.yml:74`): LAN-only mitigates;
  flag for hardening if external access is ever enabled.
- **S5 — Supabase anon JWT hardcoded in 7 code nodes**: anon keys are public by
  design; the real exposure is S1's write grant. Fix S1 first, then migrate to `$env`.
- **S4 — SECURITY DEFINER RPCs with mutable `search_path`** (`buscar_productos`,
  `popularidad_productos`, `get_resumen_movimientos`): add `SET search_path = ''`.
- **DOC1 — README + architecture docs describe a different system** (Engram, read-only
  role, Llama/Gemini, `tasas` table): actively misleads maintainers; rewrite for next session.
- **S6 → now plan 006** (done).
- **P1 → now plan 008** (done).
- **C2 → now plan 007** (done).

## Findings considered and rejected

- **Rotate the anon key as a standalone fix** — rejected. Supabase anon keys are
  publishable by design; rotating without fixing S1's write-RLS changes nothing about
  the exposure and breaks every deployment. The leverage is in S1 (RLS), not the key.
- **"SECURITY DEFINER function executable by anon" advisor warnings** (for
  `buscar_productos`, `popularidad_productos`) — down-ranked to hardening (folded into
  S4). The bot legitimately calls these via anon; they are intended to be callable.
- **Permissive write-RLS on `chat_sessions`, `clientes_chat`, `mensajes_procesados`** —
  excluded from S1's scope. These are low-value bot-state tables the anon path
  legitimately writes; the high-value target is catalog/prices/sales/rate.
- **`pg_trgm` / `pg_net` installed in `public` schema** (advisor `extension_in_public`)
  — noise for this project; not worth a migration.

## What was not audited

Direct audit by the advisor (no subagents). Not covered: the 40+ individual
`scripts/patch_*.js`; the full Supabase **performance** advisor output (saved to a
tool-results file, not fully parsed — the search columns already have the
btree+trigram indexes they need); a live diff against the running n8n instance (the
`n8n-mcp` server was disconnected, so workflow staleness was confirmed via
`scratch_live` + grep rather than a live pull); and the separate employee-app /
`backend serrucho` project that shares this Supabase database.
