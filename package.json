{
  "name": "whatsapp-sales-agent",
  "version": "1.0.0",
  "description": "Production-ready, open-source WhatsApp AI Sales & Support Agent (n8n + RAG + WAHA + Supabase + CRM)",
  "main": "setup.js",
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "start": "npm --prefix dashboard run start",
    "dashboard": "npm --prefix dashboard run dev",
    "dashboard:build": "npm --prefix dashboard run build",
    "test": "node --test \"tests/**/*.test.js\" && node scripts/check_sources_sync.js && node scripts/check_workflow_sync.js && npm --prefix dashboard run typecheck && npm --prefix dashboard run test",
    "test:recovery": "node --test tests/recovery_flow.test.js",
    "lint:recovery": "node --check scripts/catchup_engine.js scripts/lib/catchup_logic.js scripts/schemas/recovery_schemas.js tests/recovery_flow.test.js",
    "test:dashboard": "npm --prefix dashboard run test",
    "test:e2e": "npm --prefix dashboard run test:e2e",
    "pretest:live": "node scripts/check_live_env.js",
    "test:live": "node rag.js suite",
    "test:live:fast": "node rag.js suite --rapida",
    "lock": "npm install --package-lock-only --os=linux --cpu=x64 && cd dashboard && npm install --package-lock-only --os=linux --cpu=x64 && cd .."
  },
  "keywords": [
    "whatsapp",
    "agent",
    "n8n",
    "supabase",
    "engram",
    "waha"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {}
}
