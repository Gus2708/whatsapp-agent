# ── Entorno y Secretos ──
.env
.env.local
.env.*.local

# ── Dependencias de Node ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# ── Logs ──
*.log
logs/
waha_watchdog.log
boot_serrucho.log
catchup_serrucho.log

# ── Docker ──
.docker/
waha_session_storage/
n8n_local_storage/

# ── Caché y artefactos auto-generados ──
.atl/
.agents/skills/*/.skill-registry.cache.json
skills-lock.json

# ── Directorios de trabajo local ──
scratch/
scratch_live/

# ── QR / imágenes temporales ──
qr_whatsapp.png

# ── MCP config local ──
.mcp.json

# ── Sistema Operativo ──
Thumbs.db
Desktop.ini
.DS_Store
